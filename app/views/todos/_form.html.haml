= form_with(model: todo) do |form|
  - if todo.errors.any?
    #error_explanation
      %h2
        = pluralize(todo.errors.count, "error")
        prohibited this todo from being saved:
      %ul
        - todo.errors.each do |error|
          %li= error.full_message
  .field
    = form.label :title
    = form.text_field :title
  .field
    = form.label :done
    = form.check_box :done
  .field
    = form.label :count
    = form.number_field :count, value: 0
  .actions
    = form.submit
